#!/bin/sh


first_session=$(tmux ls -F '#{session_created} #{session_name}' | sort | cut -d' ' -f2- | head -n 1)
if [[ "$(tmux display -p "#S")" != "$first_session" ]]; then
    tmux switch-client -p
fi
